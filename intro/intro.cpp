#include <iostream>

// This statement brings all names from the 'std' namespace into the global scope.
using namespace std; 

int main() {
    // No need for 'std::' prefix now
    cout << "Hello, C++ World!" << endl;
    
    return 0; 
}
